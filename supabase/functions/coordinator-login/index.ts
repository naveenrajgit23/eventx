import {createClient} from 'https://esm.sh/@supabase/supabase-js@2';import {corsHeaders,json} from '../_shared/cors.ts';import {admin} from '../_shared/session.ts';
Deno.serve(async req=>{if(req.method==='OPTIONS')return new Response('ok',{headers:corsHeaders});try{const {identifier,password}=await req.json();const service=admin();const {data:email}=await service.rpc('coordinator_email_for_username',{lookup_username:String(identifier).trim()});if(!email)return json({error:'invalid_credentials'},401);const client=createClient(Deno.env.get('SUPABASE_URL')!,Deno.env.get('SUPABASE_ANON_KEY')!,{auth:{persistSession:false}});const {data,error}=await client.auth.signInWithPassword({email,password});if(error||data.user?.user_metadata?.role!=='coordinator')return json({error:'invalid_credentials'},401);return json({access_token:data.session.access_token,refresh_token:data.session.refresh_token})}catch(error){console.error(error);return json({error:'invalid_credentials'},401)}})
