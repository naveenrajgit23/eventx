import{mkdirSync,cpSync,writeFileSync}from'node:fs';import{resolve}from'node:path';
const root=resolve(import.meta.dirname,'..'),dist=resolve(root,'dist');mkdirSync(resolve(dist,'client'),{recursive:true});for(const name of ['assets','pages','index.html'])cpSync(resolve(dist,name),resolve(dist,'client',name),{recursive:true});mkdirSync(resolve(dist,'server'),{recursive:true});writeFileSync(resolve(dist,'server','index.js'),`export default {async fetch(request,env){const url=new URL(request.url);if(url.pathname==='/')url.pathname='/index.html';return env.ASSETS.fetch(new Request(url,request))}};\n`);
